#pragma once

namespace samples
{
	// TODO
	// Complte Swap() method in lecture slide (p59)
	// Call Swap() in CallReferenceExample();
	// Print the numbers after swapping
	void CallReferenceExample();
	void Swap(int& number1, int& number2);
}